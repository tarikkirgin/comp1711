#include <stdio.h>

//area of a rectangle
int main() {
    float length;
    float width;
    float area;
    printf("Enter the length of the rectangle: ");
    scanf("%f", &length);
    printf("Enter the length of the rectangle: ");
    scanf("%f", &width);
    printf("The area of the rectangle is: %f\n", length*width);
}